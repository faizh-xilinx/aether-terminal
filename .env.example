# Cursor SDK key (https://cursor.com/dashboard/cloud-agents)
# Optional at startup; required to use AI features.
CURSOR_API_KEY=

# Optional: override the model the sidecar uses.
AETHER_AI_MODEL=composer-2
